# Quick Notes

1. Make sure you have Python 3.10+ installed.
2. Install dependencies:

```bash
pip install -r requirements.txt
```

3. Run the overlay:

```bash
python main.py
```

> **Privacy:** This script listens to your key presses locally but **does not send any data online** (im just saying before people call it a virus, you can look thru the code).
